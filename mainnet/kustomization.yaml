namespace: eth-node
bases:
- ../base
resources:
- pvc.yaml
namePrefix: mainnet-
patches:
- path: deploy-patch.yaml
  target:
    kind: Deployment
    name: openethereum
configMapGenerator:
- name: geth-config
  files:
  - config.toml
  options:
    disableNameSuffixHash: true
    labels:
      app: geth
